/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -h X11/Intrinsic.h -f OClass.h -n CiBindOClass -o ../gen/OClass_B.c

--------------------------------------------*/

#include <X11/Intrinsic.h>
#include <OClass.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOClass (char* library);

static void CiBindOClassInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOClassSetupFunction (CiRoutine* switchFunction);

static void CiBindOClassSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOClassCleanup ();

static CiRoutine CiBindOClassFileCompile       = 0;
static CiRoutine CiBindOClassFunctionBind      = 0;
static CiRoutine CiBindOClassVariableBind      = 0;
static CiRoutine CiBindOClassFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOClass (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOClassFunctionSwitchNew)
    {
      CiBindOClassFunctionSwitchNew ((CiRoutine) CiBindOClassSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOClassInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOClassInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOClassFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOClassFunctionBind      = bindFunction;
  CiBindOClassVariableBind      = bindVariable;
  CiBindOClassFunctionSwitchNew = switchNew;
  CiBindOClassVariableBind (0, 0);
  CiBindOClassFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOClassSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    1, 6, 6, 0,
    6, 6, 6, 6, 6, 6, 6, 0,
    6, 6, 6, 0,
    1, 0,
    6, 0,
    6, 6, 6, 6, 4, 0,
    6, 6, 0,
    4, 6, 6, 6, 6, 6, 0,
    6, 6, 6, 6, 0,
    4, 6, 0,
    1, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOClassFileCompile ("X11/Intrinsic.h");
      CiBindOClassFileCompile ("OClass.h");
      CiBindOClassFunctionBind ("OClassGetPS_Function", (CiRoutine) OClassGetPS_Function);
      CiBindOClassFunctionBind ("OClassSetTargetChangedFunction", (CiRoutine) OClassSetTargetChangedFunction);
      CiBindOClassFunctionBind ("OClassGetPublicHeaderFileName", (CiRoutine) OClassGetPublicHeaderFileName);
      CiBindOClassFunctionBind ("OClassGetIdentifier", (CiRoutine) OClassGetIdentifier);
      CiBindOClassFunctionBind ("OClassGetIdentifiers", (CiRoutine) OClassGetIdentifiers);
      CiBindOClassFunctionBind ("OClassDeclareCompoundWidget", (CiRoutine) OClassDeclareCompoundWidget);
      CiBindOClassFunctionBind ("OClassCreate", (CiRoutine) OClassCreate);
      CiBindOClassFunctionBind ("OClassGetWidgetClassIdentifier", (CiRoutine) OClassGetWidgetClassIdentifier);
      CiBindOClassFunctionBind ("OClassIsWidgetClassValid", (CiRoutine) OClassIsWidgetClassValid);
      CiBindOClassFunctionBind ("OClassSetClearClassFunction", (CiRoutine) OClassSetClearClassFunction);
      CiBindOClassFunctionBind ("OClassDeclareWidgetClass", (CiRoutine) OClassDeclareWidgetClass);
      CiBindOClassFunctionBind ("OClassGetCreateFunction", (CiRoutine) OClassGetCreateFunction);
      CiBindOClassFunctionBind ("OClassGetCreateFunctionName", (CiRoutine) OClassGetCreateFunctionName);
      CiBindOClassFunctionBind ("OClassGetGroup", (CiRoutine) OClassGetGroup);
      CiBindOClassFunctionBind ("OClassIsShell", (CiRoutine) OClassIsShell);
      CiBindOClassFunctionBind ("OClassGetName", (CiRoutine) OClassGetName);
      CiBindOClassFunctionBind ("OClassGetTargetChangedFunction", (CiRoutine) OClassGetTargetChangedFunction);
      CiBindOClassFunctionBind ("OClassGetAttribute", (CiRoutine) OClassGetAttribute);
      CiBindOClassFunctionBind ("OClassMake", (CiRoutine) OClassMake);
      CiBindOClassFunctionBind ("OClassClearClass", (CiRoutine) OClassClearClass);
      CiBindOClassFunctionBind ("OClassGetWidgetClass", (CiRoutine) OClassGetWidgetClass);
      CiBindOClassFunctionBind ("OClassSetPS_Function", (CiRoutine) OClassSetPS_Function);
    }
  *switchFunction = (CiRoutine) CiBindOClassSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOClassSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 4 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 5 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p, args[3].p, 
                                args[4].p, args[5].p);
          }
          break;
      }
      break;
    case 9 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 19 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g ();
          }
          break;
      }
      break;
    case 21 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p, args[3].i);
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
      }
      break;
    case 24 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].p);
          }
          break;
      }
      break;
    case 36 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p);
          }
          break;
        case 2 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -f OCyclic.h -n CiBindOCyclic -o ../gen/OCyclic_B.c

--------------------------------------------*/

#include <OCyclic.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOCyclic (char* library);

static void CiBindOCyclicInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOCyclicSetupFunction (CiRoutine* switchFunction);

static void CiBindOCyclicSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOCyclicCleanup ();

static CiRoutine CiBindOCyclicFileCompile       = 0;
static CiRoutine CiBindOCyclicFunctionBind      = 0;
static CiRoutine CiBindOCyclicVariableBind      = 0;
static CiRoutine CiBindOCyclicFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOCyclic (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOCyclicFunctionSwitchNew)
    {
      CiBindOCyclicFunctionSwitchNew ((CiRoutine) CiBindOCyclicSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOCyclicInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOCyclicInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOCyclicFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOCyclicFunctionBind      = bindFunction;
  CiBindOCyclicVariableBind      = bindVariable;
  CiBindOCyclicFunctionSwitchNew = switchNew;
  CiBindOCyclicVariableBind (0, 0);
  CiBindOCyclicFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOCyclicSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    1, 6, 4, 0,
    1, 6, 6, 0,
    1, 0,
    6, 0,
    4, 6, 6, 6, 4, 6, 6, 6, 0,
    6, 6, 0,
    4, 6, 6, 6, 6, 4, 0,
    4, 6, 6, 6, 6, 6, 0,
    4, 6, 0,
    1, 6, 0,
    6, 6, 6, 4, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOCyclicFileCompile ("OCyclic.h");
      CiBindOCyclicFunctionBind ("OCyclicIsValid", (CiRoutine) OCyclicIsValid);
      CiBindOCyclicFunctionBind ("OCyclicGetName", (CiRoutine) OCyclicGetName);
      CiBindOCyclicFunctionBind ("OCyclicSetAttribute", (CiRoutine) OCyclicSetAttribute);
      CiBindOCyclicFunctionBind ("OCyclicMake", (CiRoutine) OCyclicMake);
      CiBindOCyclicFunctionBind ("OCyclicSetDelay", (CiRoutine) OCyclicSetDelay);
      CiBindOCyclicFunctionBind ("OCyclicDoMethod", (CiRoutine) OCyclicDoMethod);
      CiBindOCyclicFunctionBind ("OCyclicGetIdentifier", (CiRoutine) OCyclicGetIdentifier);
      CiBindOCyclicFunctionBind ("OCyclicGetIdentifiers", (CiRoutine) OCyclicGetIdentifiers);
      CiBindOCyclicFunctionBind ("OCyclicSetWidgetName", (CiRoutine) OCyclicSetWidgetName);
      CiBindOCyclicFunctionBind ("OCyclicDelete", (CiRoutine) OCyclicDelete);
      CiBindOCyclicFunctionBind ("OCyclicSetName", (CiRoutine) OCyclicSetName);
      CiBindOCyclicFunctionBind ("OCyclicGetAttribute", (CiRoutine) OCyclicGetAttribute);
      CiBindOCyclicFunctionBind ("OCyclicStop", (CiRoutine) OCyclicStop);
      CiBindOCyclicFunctionBind ("OCyclicStart", (CiRoutine) OCyclicStart);
      CiBindOCyclicFunctionBind ("OCyclicCreate", (CiRoutine) OCyclicCreate);
      CiBindOCyclicFunctionBind ("OCyclicSetScript", (CiRoutine) OCyclicSetScript);
      CiBindOCyclicFunctionBind ("OCyclicClearClass", (CiRoutine) OCyclicClearClass);
    }
  *switchFunction = (CiRoutine) CiBindOCyclicSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOCyclicSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 2 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].i);
          }
          break;
      }
      break;
    case 4 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 19 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g ();
          }
          break;
      }
      break;
    case 20 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].i, 
                        args[4].p, args[5].p, args[6].p);
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
        case 2 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].i);
          }
          break;
      }
      break;
    case 24 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].p);
          }
          break;
      }
      break;
    case 36 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
        case 2 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].i, args[3].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -h X11/Intrinsic.h -f OEvent.h -n CiBindOEvent -o ../gen/OEvent_B.c

--------------------------------------------*/

#include <X11/Intrinsic.h>
#include <OEvent.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOEvent (char* library);

static void CiBindOEventInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOEventSetupFunction (CiRoutine* switchFunction);

static void CiBindOEventSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOEventCleanup ();

static CiRoutine CiBindOEventFileCompile       = 0;
static CiRoutine CiBindOEventFunctionBind      = 0;
static CiRoutine CiBindOEventVariableBind      = 0;
static CiRoutine CiBindOEventFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOEvent (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOEventFunctionSwitchNew)
    {
      CiBindOEventFunctionSwitchNew ((CiRoutine) CiBindOEventSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOEventInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOEventInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOEventFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOEventFunctionBind      = bindFunction;
  CiBindOEventVariableBind      = bindVariable;
  CiBindOEventFunctionSwitchNew = switchNew;
  CiBindOEventVariableBind (0, 0);
  CiBindOEventFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOEventSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    1, 6, 4, 0,
    1, 6, 6, 0,
    1, 0,
    6, 0,
    6, 6, 0,
    4, 6, 6, 0,
    4, 6, 0,
    1, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOEventFileCompile ("X11/Intrinsic.h");
      CiBindOEventFileCompile ("OEvent.h");
      CiBindOEventFunctionBind ("OEventIsPrivateEvent", (CiRoutine) OEventIsPrivateEvent);
      CiBindOEventFunctionBind ("OEventClearClass", (CiRoutine) OEventClearClass);
      CiBindOEventFunctionBind ("OEventDispatchGrab", (CiRoutine) OEventDispatchGrab);
      CiBindOEventFunctionBind ("OEventSetEvent", (CiRoutine) OEventSetEvent);
      CiBindOEventFunctionBind ("OEventSend", (CiRoutine) OEventSend);
      CiBindOEventFunctionBind ("OEventGetEvent", (CiRoutine) OEventGetEvent);
      CiBindOEventFunctionBind ("OEventMake", (CiRoutine) OEventMake);
      CiBindOEventFunctionBind ("OEventSetType", (CiRoutine) OEventSetType);
      CiBindOEventFunctionBind ("OEventSetValue", (CiRoutine) OEventSetValue);
      CiBindOEventFunctionBind ("OEventGetValue", (CiRoutine) OEventGetValue);
      CiBindOEventFunctionBind ("OEventGetString", (CiRoutine) OEventGetString);
      CiBindOEventFunctionBind ("OEventDelete", (CiRoutine) OEventDelete);
      CiBindOEventFunctionBind ("OEventSetString", (CiRoutine) OEventSetString);
      CiBindOEventFunctionBind ("OEventGetWidget", (CiRoutine) OEventGetWidget);
      CiBindOEventFunctionBind ("OEventSendExit", (CiRoutine) OEventSendExit);
      CiBindOEventFunctionBind ("OEventGetType", (CiRoutine) OEventGetType);
      CiBindOEventFunctionBind ("OEventSetWidget", (CiRoutine) OEventSetWidget);
      CiBindOEventFunctionBind ("OEventIsExit", (CiRoutine) OEventIsExit);
    }
  *switchFunction = (CiRoutine) CiBindOEventSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOEventSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 2 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].i);
          }
          break;
      }
      break;
    case 4 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 19 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g ();
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
      }
      break;
    case 31 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 36 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -h CList.h -h OTrace.h -f OInterface.h -n CiBindOInterface -o ../gen/OInterface_B.c

--------------------------------------------*/

#include <CList.h>
#include <OTrace.h>
#include <OInterface.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOInterface (char* library);

static void CiBindOInterfaceInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOInterfaceSetupFunction (CiRoutine* switchFunction);

static void CiBindOInterfaceSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOInterfaceCleanup ();

static CiRoutine CiBindOInterfaceFileCompile       = 0;
static CiRoutine CiBindOInterfaceFunctionBind      = 0;
static CiRoutine CiBindOInterfaceVariableBind      = 0;
static CiRoutine CiBindOInterfaceFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOInterface (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOInterfaceFunctionSwitchNew)
    {
      CiBindOInterfaceFunctionSwitchNew ((CiRoutine) CiBindOInterfaceSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOInterfaceInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOInterfaceInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOInterfaceFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOInterfaceFunctionBind      = bindFunction;
  CiBindOInterfaceVariableBind      = bindVariable;
  CiBindOInterfaceFunctionSwitchNew = switchNew;
  CiBindOInterfaceVariableBind (0, 0);
  CiBindOInterfaceFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOInterfaceSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    1, 6, 4, 0,
    1, 6, 6, 0,
    6, 6, 4, 6, 6, 0,
    6, 6, 6, 0,
    6, 6, 6, 6, 6, 6, 0,
    1, 0,
    4, 0,
    6, 0,
    4, 6, 6, 6, 4, 6, 6, 6, 0,
    4, 6, 6, 6, 6, 4, 0,
    6, 6, 0,
    4, 6, 6, 6, 6, 6, 0,
    1, 4, 0,
    4, 6, 0,
    1, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOInterfaceFileCompile ("CList.h");
      CiBindOInterfaceFileCompile ("OTrace.h");
      CiBindOInterfaceFileCompile ("OInterface.h");
      CiBindOInterfaceFunctionBind ("OInterfaceSetVersion", (CiRoutine) OInterfaceSetVersion);
      CiBindOInterfaceFunctionBind ("OInterfaceVerifySyntaxOfScripts", (CiRoutine) OInterfaceVerifySyntaxOfScripts);
      CiBindOInterfaceFunctionBind ("OInterfaceMapShells", (CiRoutine) OInterfaceMapShells);
      CiBindOInterfaceFunctionBind ("OInterfaceSetAttribute", (CiRoutine) OInterfaceSetAttribute);
      CiBindOInterfaceFunctionBind ("OInterfaceDelete", (CiRoutine) OInterfaceDelete);
      CiBindOInterfaceFunctionBind ("OInterfaceRealize", (CiRoutine) OInterfaceRealize);
      CiBindOInterfaceFunctionBind ("OInterfaceDestroy", (CiRoutine) OInterfaceDestroy);
      CiBindOInterfaceFunctionBind ("OInterfaceGetParent", (CiRoutine) OInterfaceGetParent);
      CiBindOInterfaceFunctionBind ("OInterfaceSetPackages", (CiRoutine) OInterfaceSetPackages);
      CiBindOInterfaceFunctionBind ("OInterfaceIsExtentModified", (CiRoutine) OInterfaceIsExtentModified);
      CiBindOInterfaceFunctionBind ("OInterfaceSaveExtent", (CiRoutine) OInterfaceSaveExtent);
      CiBindOInterfaceFunctionBind ("OInterfaceClearClass", (CiRoutine) OInterfaceClearClass);
      CiBindOInterfaceFunctionBind ("OInterfaceIsValid", (CiRoutine) OInterfaceIsValid);
      CiBindOInterfaceFunctionBind ("OInterfaceGetName", (CiRoutine) OInterfaceGetName);
      CiBindOInterfaceFunctionBind ("OInterfaceSetExtentNotModified", (CiRoutine) OInterfaceSetExtentNotModified);
      CiBindOInterfaceFunctionBind ("OInterfaceMake", (CiRoutine) OInterfaceMake);
      CiBindOInterfaceFunctionBind ("OInterfaceSetParent", (CiRoutine) OInterfaceSetParent);
      CiBindOInterfaceFunctionBind ("OInterfaceSetFileName", (CiRoutine) OInterfaceSetFileName);
      CiBindOInterfaceFunctionBind ("OInterfaceSetVolatile", (CiRoutine) OInterfaceSetVolatile);
      CiBindOInterfaceFunctionBind ("OInterfaceGetCreateCallbackScript", (CiRoutine) OInterfaceGetCreateCallbackScript);
      CiBindOInterfaceFunctionBind ("OInterfaceGetIdentifiers", (CiRoutine) OInterfaceGetIdentifiers);
      CiBindOInterfaceFunctionBind ("OInterfaceSetDestroyCallbackScript", (CiRoutine) OInterfaceSetDestroyCallbackScript);
      CiBindOInterfaceFunctionBind ("OInterfaceSetResources", (CiRoutine) OInterfaceSetResources);
      CiBindOInterfaceFunctionBind ("OInterfaceGetPrefix", (CiRoutine) OInterfaceGetPrefix);
      CiBindOInterfaceFunctionBind ("OInterfaceGetVersion", (CiRoutine) OInterfaceGetVersion);
      CiBindOInterfaceFunctionBind ("OPieceConstruct", (CiRoutine) OPieceConstruct);
      CiBindOInterfaceFunctionBind ("OInterfaceVerifyScriptSyntaxOfExtent", (CiRoutine) OInterfaceVerifyScriptSyntaxOfExtent);
      CiBindOInterfaceFunctionBind ("OInterfaceGetPackages", (CiRoutine) OInterfaceGetPackages);
      CiBindOInterfaceFunctionBind ("OInterfaceSetCreateCallbackScript", (CiRoutine) OInterfaceSetCreateCallbackScript);
      CiBindOInterfaceFunctionBind ("OInterfaceIsModified", (CiRoutine) OInterfaceIsModified);
      CiBindOInterfaceFunctionBind ("OInterfaceGetInformations", (CiRoutine) OInterfaceGetInformations);
      CiBindOInterfaceFunctionBind ("OInterfaceSetName", (CiRoutine) OInterfaceSetName);
      CiBindOInterfaceFunctionBind ("OInterfaceGetAttribute", (CiRoutine) OInterfaceGetAttribute);
      CiBindOInterfaceFunctionBind ("OInterfaceGetDestroyCallbackScript", (CiRoutine) OInterfaceGetDestroyCallbackScript);
      CiBindOInterfaceFunctionBind ("OInterfaceSetPrefix", (CiRoutine) OInterfaceSetPrefix);
      CiBindOInterfaceFunctionBind ("OInterfaceGetIdentifier", (CiRoutine) OInterfaceGetIdentifier);
      CiBindOInterfaceFunctionBind ("OInterfaceIsPackageKnown", (CiRoutine) OInterfaceIsPackageKnown);
      CiBindOInterfaceFunctionBind ("OInterfaceSetTrace", (CiRoutine) OInterfaceSetTrace);
      CiBindOInterfaceFunctionBind ("OInterfaceDoMethod", (CiRoutine) OInterfaceDoMethod);
      CiBindOInterfaceFunctionBind ("OInterfaceGetFileName", (CiRoutine) OInterfaceGetFileName);
      CiBindOInterfaceFunctionBind ("OInterfaceAddPackage", (CiRoutine) OInterfaceAddPackage);
      CiBindOInterfaceFunctionBind ("OPieceSetReferences", (CiRoutine) OPieceSetReferences);
      CiBindOInterfaceFunctionBind ("OInterfaceMark", (CiRoutine) OInterfaceMark);
      CiBindOInterfaceFunctionBind ("OInterfaceValidate", (CiRoutine) OInterfaceValidate);
      CiBindOInterfaceFunctionBind ("OInterfaceSave", (CiRoutine) OInterfaceSave);
      CiBindOInterfaceFunctionBind ("OInterfaceLoadFile", (CiRoutine) OInterfaceLoadFile);
      CiBindOInterfaceFunctionBind ("OInterfaceCreate", (CiRoutine) OInterfaceCreate);
    }
  *switchFunction = (CiRoutine) CiBindOInterfaceSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOInterfaceSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 2 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].i);
          }
          break;
      }
      break;
    case 4 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 5 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].i, args[2].p, args[3].p);
          }
          break;
      }
      break;
    case 9 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 10 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p, args[3].p, 
                                args[4].p);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 17 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g ();
          }
          break;
      }
      break;
    case 19 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g ();
          }
          break;
      }
      break;
    case 20 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].i, 
                        args[4].p, args[5].p, args[6].p);
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].i);
          }
          break;
        case 2 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
      }
      break;
    case 24 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].p);
          }
          break;
      }
      break;
    case 35 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].i);
          }
          break;
      }
      break;
    case 36 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -f OInterpreter.h -n CiBindOInterpreter -o ../gen/OInterpreter_B.c

--------------------------------------------*/

#include <OInterpreter.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOInterpreter (char* library);

static void CiBindOInterpreterInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOInterpreterSetupFunction (CiRoutine* switchFunction);

static void CiBindOInterpreterSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOInterpreterCleanup ();

static CiRoutine CiBindOInterpreterFileCompile       = 0;
static CiRoutine CiBindOInterpreterFunctionBind      = 0;
static CiRoutine CiBindOInterpreterVariableBind      = 0;
static CiRoutine CiBindOInterpreterFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOInterpreter (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOInterpreterFunctionSwitchNew)
    {
      CiBindOInterpreterFunctionSwitchNew ((CiRoutine) CiBindOInterpreterSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOInterpreterInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOInterpreterInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOInterpreterFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOInterpreterFunctionBind      = bindFunction;
  CiBindOInterpreterVariableBind      = bindVariable;
  CiBindOInterpreterFunctionSwitchNew = switchNew;
  CiBindOInterpreterVariableBind (0, 0);
  CiBindOInterpreterFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOInterpreterSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    6, 6, 6, 0,
    6, 6, 6, 6, 6, 6, 0,
    1, 0,
    6, 0,
    6, 6, 0,
    4, 6, 6, 6, 6, 6, 0,
    4, 6, 6, 0,
    1, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOInterpreterFileCompile ("OInterpreter.h");
      CiBindOInterpreterFunctionBind ("OInterpreterClearClass", (CiRoutine) OInterpreterClearClass);
      CiBindOInterpreterFunctionBind ("OInterpreterGetAttribute", (CiRoutine) OInterpreterGetAttribute);
      CiBindOInterpreterFunctionBind ("OInterpreterGetName", (CiRoutine) OInterpreterGetName);
      CiBindOInterpreterFunctionBind ("OInterpreterGetIdentifiers", (CiRoutine) OInterpreterGetIdentifiers);
      CiBindOInterpreterFunctionBind ("OScriptIsBackgroundScript", (CiRoutine) OScriptIsBackgroundScript);
      CiBindOInterpreterFunctionBind ("OScriptGenerateCallbackCode", (CiRoutine) OScriptGenerateCallbackCode);
      CiBindOInterpreterFunctionBind ("OInterpreterMake", (CiRoutine) OInterpreterMake);
      CiBindOInterpreterFunctionBind ("OInterpreterAddInterpreter", (CiRoutine) OInterpreterAddInterpreter);
      CiBindOInterpreterFunctionBind ("OScriptExecute", (CiRoutine) OScriptExecute);
      CiBindOInterpreterFunctionBind ("OInterpreterCreate", (CiRoutine) OInterpreterCreate);
      CiBindOInterpreterFunctionBind ("OScriptVerifySyntax", (CiRoutine) OScriptVerifySyntax);
    }
  *switchFunction = (CiRoutine) CiBindOInterpreterSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOInterpreterSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 9 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 10 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p, args[3].p, 
                                args[4].p);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 19 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g ();
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
      }
      break;
    case 24 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].p);
          }
          break;
      }
      break;
    case 31 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -f OPart.h -n CiBindOPart -o ../gen/OPart_B.c

--------------------------------------------*/

#include <OPart.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOPart (char* library);

static void CiBindOPartInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOPartSetupFunction (CiRoutine* switchFunction);

static void CiBindOPartSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOPartCleanup ();

static CiRoutine CiBindOPartFileCompile       = 0;
static CiRoutine CiBindOPartFunctionBind      = 0;
static CiRoutine CiBindOPartVariableBind      = 0;
static CiRoutine CiBindOPartFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOPart (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOPartFunctionSwitchNew)
    {
      CiBindOPartFunctionSwitchNew ((CiRoutine) CiBindOPartSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOPartInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOPartInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOPartFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOPartFunctionBind      = bindFunction;
  CiBindOPartVariableBind      = bindVariable;
  CiBindOPartFunctionSwitchNew = switchNew;
  CiBindOPartVariableBind (0, 0);
  CiBindOPartFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOPartSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    1, 6, 4, 4, 0,
    1, 0,
    1, 6, 6, 6, 6, 4, 4, 0,
    6, 6, 0,
    1, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOPartFileCompile ("OPart.h");
      CiBindOPartFunctionBind ("OPartGetIdentifierFromWidget", (CiRoutine) OPartGetIdentifierFromWidget);
      CiBindOPartFunctionBind ("OPartGetIdentifier", (CiRoutine) OPartGetIdentifier);
      CiBindOPartFunctionBind ("OPartCreate", (CiRoutine) OPartCreate);
      CiBindOPartFunctionBind ("OPartGetWidget", (CiRoutine) OPartGetWidget);
      CiBindOPartFunctionBind ("OPartDelete", (CiRoutine) OPartDelete);
      CiBindOPartFunctionBind ("OPartMarkIthResource", (CiRoutine) OPartMarkIthResource);
      CiBindOPartFunctionBind ("OPartClearClass", (CiRoutine) OPartClearClass);
      CiBindOPartFunctionBind ("OPartInitialize", (CiRoutine) OPartInitialize);
    }
  *switchFunction = (CiRoutine) CiBindOPartSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOPartSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 7 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].i, args[2].i);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 20 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p, args[3].p, 
               args[4].i, args[5].i);
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -h stdarg.h -f OPiece.h -n CiBindOPiece -o ../gen/OPiece_B.c

--------------------------------------------*/

#include <stdarg.h>
#include <OPiece.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOPiece (char* library);

static void CiBindOPieceInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOPieceSetupFunction (CiRoutine* switchFunction);

static void CiBindOPieceSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOPieceCleanup ();

static CiRoutine CiBindOPieceFileCompile       = 0;
static CiRoutine CiBindOPieceFunctionBind      = 0;
static CiRoutine CiBindOPieceVariableBind      = 0;
static CiRoutine CiBindOPieceFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOPiece (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOPieceFunctionSwitchNew)
    {
      CiBindOPieceFunctionSwitchNew ((CiRoutine) CiBindOPieceSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOPieceInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOPieceInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOPieceFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOPieceFunctionBind      = bindFunction;
  CiBindOPieceVariableBind      = bindVariable;
  CiBindOPieceFunctionSwitchNew = switchNew;
  CiBindOPieceVariableBind (0, 0);
  CiBindOPieceFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOPieceSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    1, 6, 4, 0,
    1, 6, 6, 0,
    6, 6, 6, 0,
    1, 0,
    6, 0,
    6, 6, 0,
    4, 6, 6, 6, 0,
    4, 6, 6, 0,
    1, 6, 6, 6, 0,
    6, 6, 6, 6, 0,
    4, 6, 0,
    1, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOPieceFileCompile ("stdarg.h");
      CiBindOPieceFileCompile ("OPiece.h");
      CiBindOPieceFunctionBind ("OPieceBuildWidget", (CiRoutine) OPieceBuildWidget);
      CiBindOPieceFunctionBind ("OPieceGetChildren", (CiRoutine) OPieceGetChildren);
      CiBindOPieceFunctionBind ("OPieceClearClass", (CiRoutine) OPieceClearClass);
      CiBindOPieceFunctionBind ("OPieceGetName", (CiRoutine) OPieceGetName);
      CiBindOPieceFunctionBind ("OPieceAddChild", (CiRoutine) OPieceAddChild);
      CiBindOPieceFunctionBind ("OPieceDelete", (CiRoutine) OPieceDelete);
      CiBindOPieceFunctionBind ("OPieceSetDataBaseName", (CiRoutine) OPieceSetDataBaseName);
      CiBindOPieceFunctionBind ("OPieceSetVolatile", (CiRoutine) OPieceSetVolatile);
      CiBindOPieceFunctionBind ("OPieceGetType", (CiRoutine) OPieceGetType);
      CiBindOPieceFunctionBind ("OPieceGetResources", (CiRoutine) OPieceGetResources);
      CiBindOPieceFunctionBind ("OPieceGetLeaves", (CiRoutine) OPieceGetLeaves);
      CiBindOPieceFunctionBind ("OPieceAddResource", (CiRoutine) OPieceAddResource);
      CiBindOPieceFunctionBind ("OPieceGetParent", (CiRoutine) OPieceGetParent);
      CiBindOPieceFunctionBind ("OPieceGetFile", (CiRoutine) OPieceGetFile);
      CiBindOPieceFunctionBind ("OPieceSetName", (CiRoutine) OPieceSetName);
      CiBindOPieceFunctionBind ("OPieceGetIdentifiers", (CiRoutine) OPieceGetIdentifiers);
      CiBindOPieceFunctionBind ("OPieceSetType", (CiRoutine) OPieceSetType);
      CiBindOPieceFunctionBind ("OPieceGetStringOfChildren", (CiRoutine) OPieceGetStringOfChildren);
      CiBindOPieceFunctionBind ("OPieceDeleteResources", (CiRoutine) OPieceDeleteResources);
      CiBindOPieceFunctionBind ("OPieceCreate", (CiRoutine) OPieceCreate);
      CiBindOPieceFunctionBind ("OPieceIsVolatile", (CiRoutine) OPieceIsVolatile);
      CiBindOPieceFunctionBind ("OPieceConnect", (CiRoutine) OPieceConnect);
      CiBindOPieceFunctionBind ("OPieceSetParent", (CiRoutine) OPieceSetParent);
      CiBindOPieceFunctionBind ("OPieceGetIdentifier", (CiRoutine) OPieceGetIdentifier);
      CiBindOPieceFunctionBind ("OPieceSetStringOfChildren", (CiRoutine) OPieceSetStringOfChildren);
      CiBindOPieceFunctionBind ("OPieceGetDataBaseName", (CiRoutine) OPieceGetDataBaseName);
      CiBindOPieceFunctionBind ("OPieceMake", (CiRoutine) OPieceMake);
      CiBindOPieceFunctionBind ("OPieceSetVolatileChild", (CiRoutine) OPieceSetVolatileChild);
      CiBindOPieceFunctionBind ("OPieceSetFile", (CiRoutine) OPieceSetFile);
      CiBindOPieceFunctionBind ("OPieceGetWidget", (CiRoutine) OPieceGetWidget);
    }
  *switchFunction = (CiRoutine) CiBindOPieceSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOPieceSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 2 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].i);
          }
          break;
      }
      break;
    case 4 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 9 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 19 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g ();
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
      }
      break;
    case 26 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p);
          }
          break;
      }
      break;
    case 31 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 35 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p);
          }
          break;
      }
      break;
    case 36 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p);
          }
          break;
        case 2 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -h X11/Intrinsic.h -f OResource.h -n CiBindOResource -o ../gen/OResource_B.c

--------------------------------------------*/

#include <X11/Intrinsic.h>
#include <OResource.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOResource (char* library);

static void CiBindOResourceInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOResourceSetupFunction (CiRoutine* switchFunction);

static void CiBindOResourceSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOResourceCleanup ();

static CiRoutine CiBindOResourceFileCompile       = 0;
static CiRoutine CiBindOResourceFunctionBind      = 0;
static CiRoutine CiBindOResourceVariableBind      = 0;
static CiRoutine CiBindOResourceFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOResource (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOResourceFunctionSwitchNew)
    {
      CiBindOResourceFunctionSwitchNew ((CiRoutine) CiBindOResourceSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOResourceInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOResourceInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOResourceFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOResourceFunctionBind      = bindFunction;
  CiBindOResourceVariableBind      = bindVariable;
  CiBindOResourceFunctionSwitchNew = switchNew;
  CiBindOResourceVariableBind (0, 0);
  CiBindOResourceFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOResourceSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    6, 6, 6, 0,
    4, 6, 6, 6, 6, 0,
    1, 0,
    6, 6, 6, 6, 6, 0,
    4, 6, 6, 0,
    6, 6, 6, 6, 0,
    1, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOResourceFileCompile ("X11/Intrinsic.h");
      CiBindOResourceFileCompile ("OResource.h");
      CiBindOResourceFunctionBind ("OResourceGetOptions", (CiRoutine) OResourceGetOptions);
      CiBindOResourceFunctionBind ("OResourceConvertStringToChange", (CiRoutine) OResourceConvertStringToChange);
      CiBindOResourceFunctionBind ("OResourceClearClass", (CiRoutine) OResourceClearClass);
      CiBindOResourceFunctionBind ("OResourceAddConvertStringToChangeFunction", (CiRoutine) OResourceAddConvertStringToChangeFunction);
      CiBindOResourceFunctionBind ("OResourceIsAttachment", (CiRoutine) OResourceIsAttachment);
      CiBindOResourceFunctionBind ("OResourceAddGetOptionsFunction", (CiRoutine) OResourceAddGetOptionsFunction);
      CiBindOResourceFunctionBind ("OResourceIsItemList", (CiRoutine) OResourceIsItemList);
      CiBindOResourceFunctionBind ("OResourceIsFontList", (CiRoutine) OResourceIsFontList);
      CiBindOResourceFunctionBind ("OResourceIsCursor", (CiRoutine) OResourceIsCursor);
      CiBindOResourceFunctionBind ("OResourceAddGetCallbackDataFunction", (CiRoutine) OResourceAddGetCallbackDataFunction);
      CiBindOResourceFunctionBind ("OResourceIsMultiLine", (CiRoutine) OResourceIsMultiLine);
      CiBindOResourceFunctionBind ("OResourceAddConvertValueToStringFunction", (CiRoutine) OResourceAddConvertValueToStringFunction);
      CiBindOResourceFunctionBind ("OResourceIsReferenceList", (CiRoutine) OResourceIsReferenceList);
      CiBindOResourceFunctionBind ("OResourceIsPixmap", (CiRoutine) OResourceIsPixmap);
      CiBindOResourceFunctionBind ("OResourceGetCallbackData", (CiRoutine) OResourceGetCallbackData);
      CiBindOResourceFunctionBind ("OResourceIsFontStruct", (CiRoutine) OResourceIsFontStruct);
      CiBindOResourceFunctionBind ("OResourceIsFont", (CiRoutine) OResourceIsFont);
      CiBindOResourceFunctionBind ("OResourceConvertValueToString", (CiRoutine) OResourceConvertValueToString);
    }
  *switchFunction = (CiRoutine) CiBindOResourceSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOResourceSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 9 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 13 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 23 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p, args[3].p);
          }
          break;
      }
      break;
    case 31 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 36 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -f OTrace.h -n CiBindOTrace -o ../gen/OTrace_B.c

--------------------------------------------*/

#include <OTrace.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOTrace (char* library);

static void CiBindOTraceInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOTraceSetupFunction (CiRoutine* switchFunction);

static void CiBindOTraceSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOTraceCleanup ();

static CiRoutine CiBindOTraceFileCompile       = 0;
static CiRoutine CiBindOTraceFunctionBind      = 0;
static CiRoutine CiBindOTraceVariableBind      = 0;
static CiRoutine CiBindOTraceFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOTrace (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOTraceFunctionSwitchNew)
    {
      CiBindOTraceFunctionSwitchNew ((CiRoutine) CiBindOTraceSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOTraceInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOTraceInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOTraceFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOTraceFunctionBind      = bindFunction;
  CiBindOTraceVariableBind      = bindVariable;
  CiBindOTraceFunctionSwitchNew = switchNew;
  CiBindOTraceVariableBind (0, 0);
  CiBindOTraceFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOTraceSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    6, 4, 0,
    6, 6, 0,
    4, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOTraceFileCompile ("OTrace.h");
      CiBindOTraceFunctionBind ("OTraceGetIdentifier", (CiRoutine) OTraceGetIdentifier);
      CiBindOTraceFunctionBind ("OTraceGetName", (CiRoutine) OTraceGetName);
      CiBindOTraceFunctionBind ("OTraceGetNames", (CiRoutine) OTraceGetNames);
    }
  *switchFunction = (CiRoutine) CiBindOTraceSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOTraceSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 20 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].i);
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
      }
      break;
    case 36 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -h X11/Intrinsic.h -f OWidget.h -n CiBindOWidget -o ../gen/OWidget_B.c

--------------------------------------------*/

#include <X11/Intrinsic.h>
#include <OWidget.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindOWidget (char* library);

static void CiBindOWidgetInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindOWidgetSetupFunction (CiRoutine* switchFunction);

static void CiBindOWidgetSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindOWidgetCleanup ();

static CiRoutine CiBindOWidgetFileCompile       = 0;
static CiRoutine CiBindOWidgetFunctionBind      = 0;
static CiRoutine CiBindOWidgetVariableBind      = 0;
static CiRoutine CiBindOWidgetFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindOWidget (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindOWidgetFunctionSwitchNew)
    {
      CiBindOWidgetFunctionSwitchNew ((CiRoutine) CiBindOWidgetSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindOWidgetInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindOWidgetInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindOWidgetFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindOWidgetFunctionBind      = bindFunction;
  CiBindOWidgetVariableBind      = bindVariable;
  CiBindOWidgetFunctionSwitchNew = switchNew;
  CiBindOWidgetVariableBind (0, 0);
  CiBindOWidgetFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindOWidgetSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    1, 6, 2, 0,
    1, 6, 4, 0,
    1, 6, 5, 0,
    1, 6, 6, 0,
    6, 6, 4, 6, 6, 0,
    1, 6, 6, 8, 2, 0,
    6, 6, 6, 0,
    1, 6, 6, 2, 2, 0,
    2, 6, 6, 0,
    1, 0,
    2, 0,
    1, 6, 6, 6, 6, 6, 0,
    6, 6, 6, 6, 6, 4, 4, 0,
    4, 0,
    6, 0,
    6, 6, 6, 6, 2, 0,
    4, 6, 6, 6, 4, 6, 6, 6, 0,
    6, 4, 0,
    6, 6, 0,
    1, 6, 6, 6, 2, 0,
    4, 6, 6, 6, 6, 4, 0,
    1, 6, 5, 6, 0,
    1, 6, 6, 6, 4, 0,
    4, 6, 6, 6, 6, 6, 0,
    1, 6, 6, 6, 6, 0,
    8, 6, 6, 0,
    4, 6, 6, 0,
    1, 6, 6, 6, 6, 2, 2, 0,
    1, 6, 6, 4, 0,
    1, 2, 0,
    6, 6, 6, 4, 0,
    1, 4, 0,
    1, 6, 6, 6, 0,
    4, 6, 0,
    1, 6, 3, 3, 2, 0,
    1, 6, 6, 4, 2, 0,
    6, 6, 6, 6, 0,
    1, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindOWidgetFileCompile ("X11/Intrinsic.h");
      CiBindOWidgetFileCompile ("OWidget.h");
      CiBindOWidgetFunctionBind ("OWidgetSetResourceFromBoolean", (CiRoutine) OWidgetSetResourceFromBoolean);
      CiBindOWidgetFunctionBind ("OWidgetGetParent", (CiRoutine) OWidgetGetParent);
      CiBindOWidgetFunctionBind ("OWidgetKillChildren", (CiRoutine) OWidgetKillChildren);
      CiBindOWidgetFunctionBind ("OWidgetConstruct", (CiRoutine) OWidgetConstruct);
      CiBindOWidgetFunctionBind ("OWidgetSetTrace", (CiRoutine) OWidgetSetTrace);
      CiBindOWidgetFunctionBind ("OWidgetDoMethod", (CiRoutine) OWidgetDoMethod);
      CiBindOWidgetFunctionBind ("OWidgetGetAndConvertResourceToWidget", (CiRoutine) OWidgetGetAndConvertResourceToWidget);
      CiBindOWidgetFunctionBind ("OWidgetRequireScriptExecution", (CiRoutine) OWidgetRequireScriptExecution);
      CiBindOWidgetFunctionBind ("OWidgetSetFile", (CiRoutine) OWidgetSetFile);
      CiBindOWidgetFunctionBind ("OWidgetGetIdentifier", (CiRoutine) OWidgetGetIdentifier);
      CiBindOWidgetFunctionBind ("OTraceGetIdentifier", (CiRoutine) OTraceGetIdentifier);
      CiBindOWidgetFunctionBind ("OWidgetMarkResource", (CiRoutine) OWidgetMarkResource);
      CiBindOWidgetFunctionBind ("OWidgetGetHelp", (CiRoutine) OWidgetGetHelp);
      CiBindOWidgetFunctionBind ("OWidgetSetWindowManagerCloseAction", (CiRoutine) OWidgetSetWindowManagerCloseAction);
      CiBindOWidgetFunctionBind ("OWidgetDelete", (CiRoutine) OWidgetDelete);
      CiBindOWidgetFunctionBind ("OWidgetGetAndConvertResourceToDouble", (CiRoutine) OWidgetGetAndConvertResourceToDouble);
      CiBindOWidgetFunctionBind ("OTraceGetName", (CiRoutine) OTraceGetName);
      CiBindOWidgetFunctionBind ("OWidgetGetMarkedResources", (CiRoutine) OWidgetGetMarkedResources);
      CiBindOWidgetFunctionBind ("OWidgetSetVolatileChild", (CiRoutine) OWidgetSetVolatileChild);
      CiBindOWidgetFunctionBind ("OWidgetAddCallback", (CiRoutine) OWidgetAddCallback);
      CiBindOWidgetFunctionBind ("OWidgetIsVolatile", (CiRoutine) OWidgetIsVolatile);
      CiBindOWidgetFunctionBind ("OWidgetSetDragAndDrop", (CiRoutine) OWidgetSetDragAndDrop);
      CiBindOWidgetFunctionBind ("OWidgetSetResourceFromDouble", (CiRoutine) OWidgetSetResourceFromDouble);
      CiBindOWidgetFunctionBind ("OWidgetDoRubberBandGrab", (CiRoutine) OWidgetDoRubberBandGrab);
      CiBindOWidgetFunctionBind ("OWidgetIsShell", (CiRoutine) OWidgetIsShell);
      CiBindOWidgetFunctionBind ("OWidgetGetFirstShell", (CiRoutine) OWidgetGetFirstShell);
      CiBindOWidgetFunctionBind ("OWidgetHasResource", (CiRoutine) OWidgetHasResource);
      CiBindOWidgetFunctionBind ("OWidgetGetAttribute", (CiRoutine) OWidgetGetAttribute);
      CiBindOWidgetFunctionBind ("OWidgetGetType", (CiRoutine) OWidgetGetType);
      CiBindOWidgetFunctionBind ("OWidgetCallCallbacks", (CiRoutine) OWidgetCallCallbacks);
      CiBindOWidgetFunctionBind ("OWidgetGetLocalTarget", (CiRoutine) OWidgetGetLocalTarget);
      CiBindOWidgetFunctionBind ("OWidgetGetPossibleChildren", (CiRoutine) OWidgetGetPossibleChildren);
      CiBindOWidgetFunctionBind ("OWidgetPutPageInPS_Stream", (CiRoutine) OWidgetPutPageInPS_Stream);
      CiBindOWidgetFunctionBind ("OWidgetBeginResourceChanges", (CiRoutine) OWidgetBeginResourceChanges);
      CiBindOWidgetFunctionBind ("OWidgetInitializeClass", (CiRoutine) OWidgetInitializeClass);
      CiBindOWidgetFunctionBind ("OWidgetGetShell", (CiRoutine) OWidgetGetShell);
      CiBindOWidgetFunctionBind ("OWidgetBuildToolkitPart", (CiRoutine) OWidgetBuildToolkitPart);
      CiBindOWidgetFunctionBind ("OWidgetSetVolatile", (CiRoutine) OWidgetSetVolatile);
      CiBindOWidgetFunctionBind ("OWidgetMove", (CiRoutine) OWidgetMove);
      CiBindOWidgetFunctionBind ("OWidgetSetResourceFromWidget", (CiRoutine) OWidgetSetResourceFromWidget);
      CiBindOWidgetFunctionBind ("OWidgetGetAndConvertResourceToInteger", (CiRoutine) OWidgetGetAndConvertResourceToInteger);
      CiBindOWidgetFunctionBind ("OWidgetSetHelp", (CiRoutine) OWidgetSetHelp);
      CiBindOWidgetFunctionBind ("OWidgetHasMappedShell", (CiRoutine) OWidgetHasMappedShell);
      CiBindOWidgetFunctionBind ("OWidgetRequireResourceScriptExecution", (CiRoutine) OWidgetRequireResourceScriptExecution);
      CiBindOWidgetFunctionBind ("OWidgetSetResourceFromInteger", (CiRoutine) OWidgetSetResourceFromInteger);
      CiBindOWidgetFunctionBind ("OWidgetGetShells", (CiRoutine) OWidgetGetShells);
      CiBindOWidgetFunctionBind ("OWidgetSendScript", (CiRoutine) OWidgetSendScript);
      CiBindOWidgetFunctionBind ("OWidgetClearClass", (CiRoutine) OWidgetClearClass);
      CiBindOWidgetFunctionBind ("OWidgetGetInformations", (CiRoutine) OWidgetGetInformations);
      CiBindOWidgetFunctionBind ("OWidgetGetFile", (CiRoutine) OWidgetGetFile);
      CiBindOWidgetFunctionBind ("OWidgetCreate", (CiRoutine) OWidgetCreate);
      CiBindOWidgetFunctionBind ("OWidgetSetAttribute", (CiRoutine) OWidgetSetAttribute);
      CiBindOWidgetFunctionBind ("OWidgetSetName", (CiRoutine) OWidgetSetName);
      CiBindOWidgetFunctionBind ("OWidgetRemoveEventHandler", (CiRoutine) OWidgetRemoveEventHandler);
      CiBindOWidgetFunctionBind ("OWidgetSetDestroyFunction", (CiRoutine) OWidgetSetDestroyFunction);
      CiBindOWidgetFunctionBind ("OWidgetBuildPrivatePart", (CiRoutine) OWidgetBuildPrivatePart);
      CiBindOWidgetFunctionBind ("OWidgetGetResources", (CiRoutine) OWidgetGetResources);
      CiBindOWidgetFunctionBind ("OWidgetVerifySyntaxOfCallbacks", (CiRoutine) OWidgetVerifySyntaxOfCallbacks);
      CiBindOWidgetFunctionBind ("OWidgetIsVolatileChild", (CiRoutine) OWidgetIsVolatileChild);
      CiBindOWidgetFunctionBind ("OWidgetDuplicateHierarchy", (CiRoutine) OWidgetDuplicateHierarchy);
      CiBindOWidgetFunctionBind ("OWidgetCommitResourceChanges", (CiRoutine) OWidgetCommitResourceChanges);
      CiBindOWidgetFunctionBind ("OWidgetFindCurrentCallbackName", (CiRoutine) OWidgetFindCurrentCallbackName);
      CiBindOWidgetFunctionBind ("OWidgetGetAndConvertResourceToString", (CiRoutine) OWidgetGetAndConvertResourceToString);
      CiBindOWidgetFunctionBind ("OWidgetAddEventHandler", (CiRoutine) OWidgetAddEventHandler);
      CiBindOWidgetFunctionBind ("OWidgetDuplicate", (CiRoutine) OWidgetDuplicate);
      CiBindOWidgetFunctionBind ("OWidgetGetThisValue", (CiRoutine) OWidgetGetThisValue);
      CiBindOWidgetFunctionBind ("OWidgetSetPrivatePart", (CiRoutine) OWidgetSetPrivatePart);
      CiBindOWidgetFunctionBind ("OTraceGetNames", (CiRoutine) OTraceGetNames);
      CiBindOWidgetFunctionBind ("OWidgetSetForcedBackgroundExecution", (CiRoutine) OWidgetSetForcedBackgroundExecution);
      CiBindOWidgetFunctionBind ("OWidgetAssignResource", (CiRoutine) OWidgetAssignResource);
      CiBindOWidgetFunctionBind ("OWidgetSetResourceFromString", (CiRoutine) OWidgetSetResourceFromString);
      CiBindOWidgetFunctionBind ("OWidgetGetAndConvertResourceToBoolean", (CiRoutine) OWidgetGetAndConvertResourceToBoolean);
      CiBindOWidgetFunctionBind ("OWidgetSaveHierarchy", (CiRoutine) OWidgetSaveHierarchy);
      CiBindOWidgetFunctionBind ("OWidgetMarkResourceToBeSaved", (CiRoutine) OWidgetMarkResourceToBeSaved);
    }
  *switchFunction = (CiRoutine) CiBindOWidgetSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindOWidgetSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 0 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].c);
          }
          break;
      }
      break;
    case 2 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].i);
          }
          break;
      }
      break;
    case 3 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].l);
          }
          break;
      }
      break;
    case 4 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 5 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].i, args[2].p, args[3].p);
          }
          break;
      }
      break;
    case 8 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].d, args[3].c);
          }
          break;
      }
      break;
    case 9 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 10 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].c, args[3].c);
          }
          break;
      }
      break;
    case 13 :
      switch (position)
      {
        case 1 :
          {
            CiCRoutine g = (CiCRoutine) f;

            prim.c = g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 15 :
      switch (position)
      {
        case 1 :
          {
            CiCRoutine g = (CiCRoutine) f;

            prim.c = g ();
          }
          break;
      }
      break;
    case 17 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p, args[3].p, 
               args[4].p);
          }
          break;
        case 2 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p, args[3].p, 
                                args[4].i, args[5].i);
          }
          break;
        case 3 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g ();
          }
          break;
      }
      break;
    case 19 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g ();
          }
          break;
        case 2 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p, args[3].c);
          }
          break;
      }
      break;
    case 20 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].i, 
                        args[4].p, args[5].p, args[6].p);
          }
          break;
        case 2 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].i);
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
        case 2 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p, args[3].c);
          }
          break;
        case 3 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].i);
          }
          break;
        case 4 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].l, args[2].p);
          }
          break;
      }
      break;
    case 24 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p, args[3].i);
          }
          break;
        case 2 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].p);
          }
          break;
      }
      break;
    case 26 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p, args[3].p);
          }
          break;
      }
      break;
    case 27 :
      switch (position)
      {
        case 1 :
          {
            CiDRoutine g = (CiDRoutine) f;

            prim.d = g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 31 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 32 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p, args[3].p, 
               args[4].c, args[5].c);
          }
          break;
      }
      break;
    case 33 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].i);
          }
          break;
        case 2 :
          {
            CiRoutine g = f;

            g (args[0].c);
          }
          break;
      }
      break;
    case 34 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].i);
          }
          break;
      }
      break;
    case 35 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].i);
          }
          break;
        case 2 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p);
          }
          break;
      }
      break;
    case 36 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p);
          }
          break;
        case 2 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].s, args[2].s, args[3].c);
          }
          break;
        case 3 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].i, args[3].c);
          }
          break;
        case 4 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p, args[1].p, args[2].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -f WoTypes.h -n CiBindWoTypes -o ../gen/WoTypes_B.c

--------------------------------------------*/

#include <WoTypes.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindWoTypes (char* library);

static void CiBindWoTypesInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindWoTypesSetupFunction (CiRoutine* switchFunction);

static void CiBindWoTypesSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindWoTypesCleanup ();

static CiRoutine CiBindWoTypesFileCompile       = 0;
static CiRoutine CiBindWoTypesFunctionBind      = 0;
static CiRoutine CiBindWoTypesVariableBind      = 0;
static CiRoutine CiBindWoTypesFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindWoTypes (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindWoTypesFunctionSwitchNew)
    {
      CiBindWoTypesFunctionSwitchNew ((CiRoutine) CiBindWoTypesSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindWoTypesInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindWoTypesInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindWoTypesFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindWoTypesFunctionBind      = bindFunction;
  CiBindWoTypesVariableBind      = bindVariable;
  CiBindWoTypesFunctionSwitchNew = switchNew;
  CiBindWoTypesVariableBind (0, 0);
  CiBindWoTypesFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindWoTypesSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    1, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindWoTypesFileCompile ("WoTypes.h");
      CiBindWoTypesFunctionBind ("WoSetTypes", (CiRoutine) WoSetTypes);
    }
  *switchFunction = (CiRoutine) CiBindWoTypesSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindWoTypesSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

/*--------------------------------------------
  This file has been generated by cigen       
  using the following command :               

  $CIBIN/cigen.exe -p -h X11/Xlib.h -h OTrace.h -h OShell.h -f Wo.h -n CiBindWo -o ../gen/Wo_B.c

--------------------------------------------*/

#include <X11/Xlib.h>
#include <OTrace.h>
#include <OShell.h>
#include <Wo.h>

#ifndef __CI_SWITCH_DEFS__
#define __CI_SWITCH_DEFS__

#ifdef __cplusplus
#define SWITCH_ELLIPSIS ...
#else
#define SWITCH_ELLIPSIS 

#endif

#ifndef __CiDefs__

typedef void            (*CiRoutine)(SWITCH_ELLIPSIS);
typedef char            (*CiCRoutine)(SWITCH_ELLIPSIS);
typedef short           (*CiSRoutine)(SWITCH_ELLIPSIS);
typedef int             (*CiIRoutine)(SWITCH_ELLIPSIS);
typedef long            (*CiLRoutine)(SWITCH_ELLIPSIS);
typedef unsigned char   (*CiUCRoutine)(SWITCH_ELLIPSIS);
typedef unsigned short  (*CiUSRoutine)(SWITCH_ELLIPSIS);
typedef unsigned int    (*CiUIRoutine)(SWITCH_ELLIPSIS);
typedef unsigned long   (*CiULRoutine)(SWITCH_ELLIPSIS);
typedef float           (*CiFRoutine)(SWITCH_ELLIPSIS);
typedef double          (*CiDRoutine)(SWITCH_ELLIPSIS);
typedef void*           (*CiPRoutine)(SWITCH_ELLIPSIS);

#endif

#ifndef __CiType__

typedef union {
  char c;
  short s;
  int i;
  long l;
  char* p;
  float f;
  double d;
} CiPrimValueRec, *CiPrimValue;

#endif

#endif

#ifdef __cplusplus
extern "C" {
#endif

CiRoutine CiBindWo (char* library);

static void CiBindWoInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew);

static char* CiBindWoSetupFunction (CiRoutine* switchFunction);

static void CiBindWoSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue);
void CiBindWoCleanup ();

static CiRoutine CiBindWoFileCompile       = 0;
static CiRoutine CiBindWoFunctionBind      = 0;
static CiRoutine CiBindWoVariableBind      = 0;
static CiRoutine CiBindWoFunctionSwitchNew = 0;

#ifdef __cplusplus
}
#endif

/*- Procedure ------------------------------------------*/
CiRoutine CiBindWo (char* library)
/*------------------------------------------------------*/
{
  static int first = 1;

  if (first)
    {
      first = 0;
    }
  else if (CiBindWoFunctionSwitchNew)
    {
      CiBindWoFunctionSwitchNew ((CiRoutine) CiBindWoSetupFunction);
    }

  library = 0;

  return ((CiRoutine) CiBindWoInitFunction);
}

/*- Procedure ------------------------------------------*/
static void CiBindWoInitFunction (CiRoutine compileFunction,
                            CiRoutine pathFunction,
                            CiRoutine bindFunction,
                            CiRoutine bindVariable,
                            CiRoutine switchNew)
/*------------------------------------------------------*/
{
  CiBindWoFileCompile       = compileFunction;
  pathFunction = 0;
  CiBindWoFunctionBind      = bindFunction;
  CiBindWoVariableBind      = bindVariable;
  CiBindWoFunctionSwitchNew = switchNew;
  CiBindWoVariableBind (0, 0);
  CiBindWoFunctionBind (0, 0);
}

/*- Procedure ------------------------------------------*/
static char* CiBindWoSetupFunction (CiRoutine* switchFunction)
/*------------------------------------------------------*/
{
  static char cases[] = {
    1, 6, 4, 0,
    1, 6, 6, 0,
    5, 6, 0,
    2, 6, 6, 0,
    1, 0,
    4, 0,
    1, 4, 6, 0,
    6, 0,
    4, 6, 6, 6, 4, 6, 6, 6, 0,
    6, 6, 0,
    4, 6, 6, 6, 6, 4, 0,
    4, 6, 6, 6, 6, 6, 0,
    1, 6, 6, 6, 6, 0,
    1, 6, 6, 4, 0,
    1, 4, 0,
    1, 6, 6, 6, 0,
    4, 6, 0,
    1, 6, 0,
    0
  };
  static int first = 1;
  
  if (first)
    {
      first = 0;
  
      CiBindWoFileCompile ("X11/Xlib.h");
      CiBindWoFileCompile ("OTrace.h");
      CiBindWoFileCompile ("OShell.h");
      CiBindWoFileCompile ("Wo.h");
      CiBindWoFunctionBind ("XtDisplay", (CiRoutine) XtDisplay);
      CiBindWoFunctionBind ("WoSetAttribute", (CiRoutine) WoSetAttribute);
      CiBindWoFunctionBind ("WoSetTrace", (CiRoutine) WoSetTrace);
      CiBindWoFunctionBind ("WoDoMethod", (CiRoutine) WoDoMethod);
      CiBindWoFunctionBind ("XtCallCallbacks", (CiRoutine) XtCallCallbacks);
      CiBindWoFunctionBind ("WoGetStopFlag", (CiRoutine) WoGetStopFlag);
      CiBindWoFunctionBind ("WoWaitClientMessage", (CiRoutine) WoWaitClientMessage);
      CiBindWoFunctionBind ("XtInstallAllAccelerators", (CiRoutine) XtInstallAllAccelerators);
      CiBindWoFunctionBind ("WoIsInitialized", (CiRoutine) WoIsInitialized);
      CiBindWoFunctionBind ("XtInstallAccelerators", (CiRoutine) XtInstallAccelerators);
      CiBindWoFunctionBind ("WoExecuteSystemScript", (CiRoutine) WoExecuteSystemScript);
      CiBindWoFunctionBind ("XtClass", (CiRoutine) XtClass);
      CiBindWoFunctionBind ("XtParent", (CiRoutine) XtParent);
      CiBindWoFunctionBind ("XtManageChild", (CiRoutine) XtManageChild);
      CiBindWoFunctionBind ("WoExecuteShellScript", (CiRoutine) WoExecuteShellScript);
      CiBindWoFunctionBind ("WoSetExitFunction", (CiRoutine) WoSetExitFunction);
      CiBindWoFunctionBind ("WoAddClearClassFunction", (CiRoutine) WoAddClearClassFunction);
      CiBindWoFunctionBind ("XtName", (CiRoutine) XtName);
      CiBindWoFunctionBind ("WoDispatchEvent", (CiRoutine) WoDispatchEvent);
      CiBindWoFunctionBind ("WoClearClass", (CiRoutine) WoClearClass);
      CiBindWoFunctionBind ("WoSetInterfaceFile", (CiRoutine) WoSetInterfaceFile);
      CiBindWoFunctionBind ("WoExecuteShellScriptInSameContext", (CiRoutine) WoExecuteShellScriptInSameContext);
      CiBindWoFunctionBind ("XtGetValues", (CiRoutine) XtGetValues);
      CiBindWoFunctionBind ("WoGetGlobalProcess", (CiRoutine) WoGetGlobalProcess);
      CiBindWoFunctionBind ("WoProcessEvents", (CiRoutine) WoProcessEvents);
      CiBindWoFunctionBind ("XtRealizeWidget", (CiRoutine) XtRealizeWidget);
      CiBindWoFunctionBind ("WoFlushEvents", (CiRoutine) WoFlushEvents);
      CiBindWoFunctionBind ("WoExecuteShellFileInSameContext", (CiRoutine) WoExecuteShellFileInSameContext);
      CiBindWoFunctionBind ("WoSetStopFlag", (CiRoutine) WoSetStopFlag);
      CiBindWoFunctionBind ("WoSendExit", (CiRoutine) WoSendExit);
      CiBindWoFunctionBind ("XtWindow", (CiRoutine) XtWindow);
      CiBindWoFunctionBind ("WoGetInterfaceFile", (CiRoutine) WoGetInterfaceFile);
      CiBindWoFunctionBind ("XtManageChildren", (CiRoutine) XtManageChildren);
      CiBindWoFunctionBind ("XtSetValues", (CiRoutine) XtSetValues);
      CiBindWoFunctionBind ("WoSetXt", (CiRoutine) WoSetXt);
      CiBindWoFunctionBind ("XtFree", (CiRoutine) XtFree);
      CiBindWoFunctionBind ("XtUnrealizeWidget", (CiRoutine) XtUnrealizeWidget);
      CiBindWoFunctionBind ("XtUnmanageChild", (CiRoutine) XtUnmanageChild);
      CiBindWoFunctionBind ("XtIsSubclass", (CiRoutine) XtIsSubclass);
      CiBindWoFunctionBind ("WoGetTrace", (CiRoutine) WoGetTrace);
      CiBindWoFunctionBind ("WoGetVersion", (CiRoutine) WoGetVersion);
      CiBindWoFunctionBind ("WoSetPasteScript", (CiRoutine) WoSetPasteScript);
      CiBindWoFunctionBind ("WoSetInitializeClassFunction", (CiRoutine) WoSetInitializeClassFunction);
      CiBindWoFunctionBind ("XtUnmanageChildren", (CiRoutine) XtUnmanageChildren);
      CiBindWoFunctionBind ("WoStartup", (CiRoutine) WoStartup);
      CiBindWoFunctionBind ("XtGetConstraintResourceList", (CiRoutine) XtGetConstraintResourceList);
      CiBindWoFunctionBind ("XtGetResourceList", (CiRoutine) XtGetResourceList);
      CiBindWoFunctionBind ("WoGetShellInterpreter", (CiRoutine) WoGetShellInterpreter);
      CiBindWoFunctionBind ("WoGetArguments", (CiRoutine) WoGetArguments);
      CiBindWoFunctionBind ("WoGetAttribute", (CiRoutine) WoGetAttribute);
    }
  *switchFunction = (CiRoutine) CiBindWoSwitchFunction;
  return ((char*) cases);
}

/*- Procedure ------------------------------------------*/
static void CiBindWoSwitchFunction (CiRoutine f,
                              int code,
                              int position,
                              CiPrimValue args,
                              CiPrimValue retValue)
/*------------------------------------------------------*/
{
  CiPrimValueRec prim;
  
  prim.l = 0;
  
  switch (code)
  {
    case 2 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].i);
          }
          break;
      }
      break;
    case 4 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 9 :
      switch (position)
      {
        case 1 :
          {
            CiLRoutine g = (CiLRoutine) f;

            prim.l = g (args[0].p);
          }
          break;
      }
      break;
    case 13 :
      switch (position)
      {
        case 1 :
          {
            CiCRoutine g = (CiCRoutine) f;

            prim.c = g (args[0].p, args[1].p);
          }
          break;
      }
      break;
    case 14 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g ();
          }
          break;
      }
      break;
    case 17 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g ();
          }
          break;
      }
      break;
    case 18 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].i, args[1].p);
          }
          break;
      }
      break;
    case 19 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g ();
          }
          break;
      }
      break;
    case 20 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].i, 
                        args[4].p, args[5].p, args[6].p);
          }
          break;
      }
      break;
    case 22 :
      switch (position)
      {
        case 1 :
          {
            CiPRoutine g = (CiPRoutine) f;

            prim.p = (char*) g (args[0].p);
          }
          break;
        case 2 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].i);
          }
          break;
      }
      break;
    case 24 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p, args[1].p, args[2].p, args[3].p, 
                        args[4].p);
          }
          break;
      }
      break;
    case 26 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p, args[3].p);
          }
          break;
      }
      break;
    case 33 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].i);
          }
          break;
      }
      break;
    case 35 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].i);
          }
          break;
        case 2 :
          {
            CiRoutine g = f;

            g (args[0].p, args[1].p, args[2].p);
          }
          break;
      }
      break;
    case 36 :
      switch (position)
      {
        case 1 :
          {
            CiIRoutine g = (CiIRoutine) f;

            prim.i = g (args[0].p);
          }
          break;
      }
      break;
    case 37 :
      switch (position)
      {
        case 1 :
          {
            CiRoutine g = f;

            g (args[0].p);
          }
          break;
      }
      break;
  }
  
  args = 0;
  position = 0;
  
  if (retValue) *retValue = prim;
}

